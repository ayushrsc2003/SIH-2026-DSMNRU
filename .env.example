# Database Connection String
# For local dev with SQLite or Postgres:
# SQLite example: "file:./dev.db" (change provider in schema.prisma if using SQLite locally)
# Managed Postgres (Neon / Supabase / Railway):
DATABASE_URL="postgresql://neondb_owner:password@ep-example-123456.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Admin Panel Security
ADMIN_PASSWORD="sih2026admin@dsmnru"

# Node Environment
NODE_ENV="development"
